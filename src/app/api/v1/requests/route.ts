import { NextResponse } from "next/server";
import { z } from "zod";
import { getCurrentUser } from "@/server/auth";
import { db } from "@/server/db";
import { audit } from "@/server/audit";
const schema=z.union([z.object({kind:z.literal("leave"),leaveTypeId:z.string(),startDate:z.coerce.date(),endDate:z.coerce.date(),requestedMinutes:z.coerce.number().int().positive(),reason:z.string().min(3),description:z.string().optional()}).refine((value)=>value.startDate<=value.endDate,{message:"تاریخ شروع باید قبل از تاریخ پایان باشد.",path:["endDate"]}),z.object({kind:z.literal("offtime"),date:z.coerce.date(),startMinutes:z.coerce.number().int().min(0).max(1439),endMinutes:z.coerce.number().int().min(1).max(1440),reason:z.string().min(3),description:z.string().optional()}).refine((value)=>value.startMinutes<value.endMinutes,{message:"زمان شروع باید قبل از زمان پایان باشد.",path:["endMinutes"]}),z.object({kind:z.literal("correction"),date:z.coerce.date(),proposedIn:z.coerce.date().optional(),proposedOut:z.coerce.date().optional(),reason:z.string().min(3),description:z.string().optional()}).refine((value)=>!value.proposedIn||!value.proposedOut||value.proposedIn<value.proposedOut,{message:"زمان ورود باید قبل از زمان خروج باشد.",path:["proposedOut"]})]);
export async function GET(){const user=await getCurrentUser();if(!user?.employee)return NextResponse.json({error:{code:"FORBIDDEN",message:"حساب کارمندی ندارید."}},{status:403});const [leave,offtime,correction]=await Promise.all([db.leaveRequest.findMany({where:{employeeId:user.employee.id},include:{leaveType:true,approval:{include:{decisions:true}}},orderBy:{createdAt:"desc"}}),db.offTimeRequest.findMany({where:{employeeId:user.employee.id},orderBy:{createdAt:"desc"}}),db.attendanceCorrectionRequest.findMany({where:{employeeId:user.employee.id},orderBy:{createdAt:"desc"}})]);return NextResponse.json({data:{leave,offtime,correction}});}
export async function POST(request:Request){const user=await getCurrentUser();if(!user?.employee)return NextResponse.json({error:{code:"FORBIDDEN",message:"حساب کارمندی ندارید."}},{status:403});const result=schema.safeParse(await request.json().catch(()=>({})));if(!result.success)return NextResponse.json({error:{code:"VALIDATION_ERROR",message:"اطلاعات درخواست معتبر نیست.",fields:result.error.flatten().fieldErrors}},{status:400});const value=result.data;try{if(value.kind==="leave"){const created=await db.leaveRequest.create({data:{companyId:user.companyId,employeeId:user.employee.id,leaveTypeId:value.leaveTypeId,startDate:value.startDate,endDate:value.endDate,requestedMinutes:value.requestedMinutes,reason:value.reason,description:value.description,status:"PENDING_MANAGER"}});await audit({companyId:user.companyId,actorId:user.id,action:"leave.request",entityType:"LeaveRequest",entityId:created.id});return NextResponse.json({data:created},{status:201});}if(value.kind==="offtime"){if(value.endMinutes<=value.startMinutes)return NextResponse.json({error:{code:"VALIDATION_ERROR",message:"زمان پایان باید بعد از شروع باشد."}},{status:400});const created=await db.offTimeRequest.create({data:{companyId:user.companyId,employeeId:user.employee.id,date:value.date,startMinutes:value.startMinutes,endMinutes:value.endMinutes,requestedMinutes:value.endMinutes-value.startMinutes,reason:value.reason,description:value.description,status:"PENDING_MANAGER"}});await audit({companyId:user.companyId,actorId:user.id,action:"offtime.request",entityType:"OffTimeRequest",entityId:created.id});return NextResponse.json({data:created},{status:201});}const created=await db.attendanceCorrectionRequest.create({data:{companyId:user.companyId,employeeId:user.employee.id,date:value.date,proposedIn:value.proposedIn,proposedOut:value.proposedOut,reason:value.reason,description:value.description,status:"PENDING_MANAGER"}});await audit({companyId:user.companyId,actorId:user.id,action:"correction.request",entityType:"AttendanceCorrectionRequest",entityId:created.id});return NextResponse.json({data:created},{status:201});}catch{return NextResponse.json({error:{code:"REQUEST_FAILED",message:"ثبت درخواست انجام نشد."}},{status:400});}}
