"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import Image from "next/image";
import styles from "./page.module.scss";
export default function LoginPage(){const router=useRouter();const [username,setUsername]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError("");const r=await fetch("/api/v1/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username,password})});const data=await r.json();setLoading(false);if(!r.ok){setError(data.error?.message||"ورود انجام نشد");return}router.push(data.data.mustChangePassword?"/change-password":"/dashboard");router.refresh()}return <main className={styles.page}><section className={styles.card}><div className={styles.brand}><span className={styles.mark}>ح</span><div><strong>سامانه حضور و مرخصی</strong><small>Hozoor</small></div></div><div className={styles.heading}><p className="eyebrow">ورود امن به سامانه</p><h1>خوش آمدید</h1><p>برای ادامه، مشخصات حساب خود را وارد کنید.</p></div><form onSubmit={submit} className="stack"><label>نام کاربری<input dir="ltr" value={username} onChange={e=>setUsername(e.target.value)} autoComplete="username" required /></label><label>رمز عبور<input dir="ltr" type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" required /></label>{error&&<div className="alert danger" role="alert">{error}</div>}<button className="button primary wide" disabled={loading}>{loading?"در حال ورود…":"ورود به سامانه"}</button></form><div className={styles.footer}>ثبت‌نام عمومی فعال نیست؛ حساب‌ها توسط مدیر سامانه ایجاد می‌شوند.</div></section></main>}
