"use client";
import { useEffect, useState } from "react";
type LeaveType = { id: string; name: string; unit: string; paid: boolean; active: boolean; requiresAttachment: boolean };
export default function LeaveTypesPage() { const [rows, setRows] = useState<LeaveType[]>([]); const [message, setMessage] = useState(""); async function load() { const response = await fetch("/api/v1/leave-types"); const result = await response.json(); if (response.ok) setRows(result.data); } useEffect(() => { void load(); }, []); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); const response = await fetch("/api/v1/leave-types", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name: form.get("name"), unit: form.get("unit"), paid: true, requiresAttachment: form.get("requiresAttachment") === "on" }) }); const result = await response.json(); setMessage(response.ok ? "نوع مرخصی ایجاد شد." : result.error?.message || "ایجاد نوع مرخصی ناموفق بود."); if (response.ok) { setRows([...rows, result.data]); event.currentTarget.reset(); } } return <><div className="sectionTitle"><div><p className="eyebrow">مدیریت مرخصی</p><h1>انواع مرخصی</h1><p>دسته‌بندی‌های قابل درخواست کارکنان را تنظیم کنید.</p></div></div><div className="layout"><form className="card stack" onSubmit={submit}><h3>افزودن نوع مرخصی</h3><label>نام<input name="name" required /></label><label>واحد<select name="unit" defaultValue="DAY"><option value="DAY">روز</option><option value="HOUR">ساعت</option></select></label><label><input name="requiresAttachment" type="checkbox" /> نیازمند پیوست</label>{message && <div className="alert success">{message}</div>}<button className="button primary">افزودن نوع مرخصی</button></form><section className="card"><h3>انواع تنظیم‌شده</h3>{rows.length ? rows.map((row) => <article key={row.id} style={{ padding: "14px 0", borderBottom: "1px solid var(--color-border-secondary)" }}><strong>{row.name}</strong><p>{row.unit === "DAY" ? "روزانه" : "ساعتی"} · {row.paid ? "با پرداخت" : "بدون پرداخت"} · {row.active ? "فعال" : "غیرفعال"}</p></article>) : <div className="empty">هنوز نوع مرخصی ثبت نشده است.</div>}</section></div></>; }
