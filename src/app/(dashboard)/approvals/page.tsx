import ApprovalsClient from "@/components/ApprovalsClient"; import { getCurrentUser } from "@/server/auth"; import { db } from "@/server/db";
export default async function Approvals(){const user=await getCurrentUser();if(!user||!["ADMIN","HR_ADMIN","MANAGER"].includes(user.role))return null;const [leave,offtime,correction]=await Promise.all([db.leaveRequest.findMany({where:{companyId:user.companyId,status:{in:["SUBMITTED","PENDING_MANAGER","PENDING_ADMIN"]}},include:{employee:true,leaveType:true},orderBy:{createdAt:"asc"}}),db.offTimeRequest.findMany({where:{companyId:user.companyId,status:{in:["SUBMITTED","PENDING_MANAGER","PENDING_ADMIN"]}},include:{employee:true},orderBy:{createdAt:"asc"}}),db.attendanceCorrectionRequest.findMany({where:{companyId:user.companyId,status:{in:["SUBMITTED","PENDING_MANAGER","PENDING_ADMIN"]}},include:{employee:true},orderBy:{createdAt:"asc"}})]);return <ApprovalsClient initial={{leave,offtime,correction}}/>}
