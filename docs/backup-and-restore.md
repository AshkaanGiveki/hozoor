# پشتیبان‌گیری

از `pg_dump` برای DB و tar از volume `hozoor-files` برای upload/attachment/export استفاده کنید. هر دو فایل را با دسترسی محدود و رمزگذاری خارج از سرور نگه دارید. restore را ابتدا روی محیط آزمایشی و با همان نسخهٔ schema بررسی کنید.
